import { Text } from "@chakra-ui/react"
import { BrowserRouter } from "react-router-dom"

export const App: React.FC = () => {
  return (
    <BrowserRouter>
      <Text>テキストテキスト</Text>
    </BrowserRouter>
  )
}
